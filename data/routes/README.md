# Surveyed route manifests

Add a route JSON file here only after recording its measurements in `docs/FIELD_SURVEY.md`. The mobile app validates the selected manifest before navigation. An empty directory keeps route guidance unavailable.
